<% if user_signed_in? %>
    <li><%= link_to t('signed_in') + " #{current_user.email}", destroy_user_session_path, :method => :delete %></li>
<% else %>
    <li><%= link_to t('sign_in'), new_user_session_path %></li>
<% end %>